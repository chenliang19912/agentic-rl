# 单卡 V100 上，我把检索 Agent 的训练栈原样搬到代码修 bug 上——并当场抓到模型背答案

> （2026-09-08 增补第九节：上文"下一步"清单里的 turn 级信用分配与推理引擎定制两项已经做完，
> GiGPO 实验把"修复通道"打开了一条缝，预注册判据 PASS。）

> 自研轻量 Agentic RL 训练栈 · 代码执行环境篇 · 完整复盘
> 所有数字真实跑出，口径见仓库 `C4_README.md`。代码 + 实验日志已开源（MIT）。
> 前传（检索环境篇）：[《单卡 V100 从零自研多轮 GRPO：把 Qwen3-4B 训成"检索—推理—再检索"Agent》](https://blog.csdn.net/weixin_49528551/article/details/164589252)，
> 本文开头提到的"检索 Agent 项目"即指它。
> 代码、实验日志（含 10 条完整坑记录）、复现命令都在仓库：[agentic-rl](https://github.com/chenliang19912/agentic-rl)。欢迎拍砖。

## 缘起：如果"可验证奖励 Agentic RL"是通用范式，那环境就应该能像插件一样换

前一个项目（检索 Agent）我在单卡 V100 上从零搭了一套多轮 GRPO 训练栈，把 Qwen3-4B 训成"检索—推理—再检索"
的多跳问答 Agent（EM 4.20→26.60）。做完之后我一直在想一个问题：**这套东西到底是"为检索特调的"，
还是一个通用范式？**

检验它最狠的办法，不是再调一个检索数据集，而是**把环境整个换掉**——从 `<search>` 换成 `<run_code>`，
从多跳问答换成"看失败测试→改代码→再执行→再提交"的多轮修码。如果训练层能**零改动**复用、只换环境接口
就把新任务训出来，那"环境可插拔"就不再是 PPT 话术，而是有代码级证据的结论。

这就是本项目。它最终跑出了三组干净的对照数字，但真正让我觉得值钱的，是路上撞见的两件事：
**① 当场抓到 Qwen3-4B 在逐字背 benchmark 答案（污染率 76.5% 定量实测）；② GRPO 的贪心评测峰值
出现在 iter1 而不是最后一轮——训练分还在涨，部署分已经见顶，而这和检索 Agent 项目是同一个现象。**

下面按这两条线讲。

---

## 一、任务与环境：~100 行无容器沙盒，以及一次差点烧掉宿主机的 bigint 爆炸

### 任务怎么造

从 MBPP+ 和 HumanEval+（EvalPlus 双源）取函数级题目，用 **AST 单点变异**往里注入一个 bug：
比较符翻转（`<`→`>`）、算术符替换（`+`→`-`）、常量 ±1、布尔取反、`and`/`or` 互换、删掉循环体里一句话。
模型的输入 = 坏代码 + 一条会失败的可见测试 + 它的报错输出；模型要循环"改→`<run_code>`执行→看 stdout→再改"，
最多 5 轮，最后用 `<answer>` 提交终版。奖励 = **隐藏测试的逐 case 通过率**（稠密，不是整题 0/1）。

### 沙盒：为什么不用 Docker

2026 年代码 Agent 圈的一个明显趋势是"去 Docker 化"（北大 SWE-MiniSandbox，ICML 2026，用 namespace/chroot
替代容器，单节点 8×B200 跑完全部实验）。我的场景更轻——单文件函数级修码，根本用不上一套容器编排：

- **禁网**：`unshare -rn` 起一个网络命名空间，物理断网（既防模型在线查答案，也防恶意代码外联）；
- **限时限内存禁写**：`RLIMIT_CPU`（超时内核级杀）+ `RLIMIT_AS`（4GB 地址空间上限）+ `RLIMIT_FSIZE=0`（禁写文件）；
- **隔离解释器**：`python -B -I`（不写字节码、隔离模式，忽略用户 site-packages 与环境变量）；
- **输出截断** 1200 字符 + 最小化 env。

整个沙盒核心约 100 行。但这里有个坑，差点把宿主机烧了。

### 三连死取证：SIGALRM 拦不住 C 级的 bigint 乘法

构建任务池时，进程**连续三次静默死亡**——没有 traceback、没有退出码、日志停在半路，表象酷似普通崩溃。
第三次我去掉 docker 的 `--rm`、同步盯着跑，`docker stats` 一打开就明白了：**CPU 100%，内存正在往 46.8GiB 冲。**

罪魁是一道 collatz 题。它的单点变异把 `3*n+1` 改成了 `3*n-1`——这个"小改动"让整数序列**指数爆炸**：
n 每一步都在翻倍增长，很快就变成几千位的大整数。而**单次大整数乘法是一个 C 级调用**，Python 的 `SIGALRM`
信号 handler 只在字节码边界执行，根本插不进一个正在进行的 C 级乘法里。于是我进程内那套"signal.alarm 超时"
的防护形同虚设，内存一路膨胀，直到触发**宿主机的 OOM killer** 把容器 SIGKILL——这就是前两次"静默死亡"的真相。

修复方案，也是我沉淀下来的一条通用教训：

> **任何"执行不可信/变异代码"的验证环节，进程内 SIGALRM 不是超时保证，rlimit 子进程才是。**

具体三层防护：
1. 把验证 exec 移进 **fork 出来的子进程**；
2. 子进程 `RLIMIT_CPU`——超时由内核发 `SIGXCPU` **直接杀**，不依赖字节码边界；
3. 子进程 `RLIMIT_AS` 4GB——bigint 一膨胀，分配失败，实测 **2.6 秒**就抛 `MemoryError`；
4. 父进程再加一道 `poll` 超时 `kill` 兜底。

还有两个配套的语义修复，很容易漏：① `MemoryError`/`RecursionError`/`OverflowError` 这类**资源型异常
必须判定为"这个变异体跑飞了、弃用"**（返回 None），绝不能当成普通的"断言失败"（False）收进任务集——
否则你就把一道"会让任何执行者 OOM 的毒题"当成正常 bug 题喂给了模型；② 逐 case 跑 plus 测试时，
`except Exception` 会把 `MemoryError` 一起吞掉，资源型异常必须 re-raise 给外层 guard 判废。

---

## 二、当场抓到模型背答案：benchmark 污染率 76.5% 的一手实测

这是整个项目我最想讲的部分，因为它是"先测污染、再筛、再训"这套方法论的现场价值。

跑基线的时候我顺手加了一道**查重门**：把模型提交的代码和参考解都归一化（折叠空白）后逐字符比对，
相同就直接判 0 分。结果第一批 8 道题冒出来 **4 道 dup**——模型在贪心解码下，把参考解**一字不差地背了出来**。

这不是个别现象。我把全池 378 题（MBPP+ 257 ∪ HumanEval+ 121）跑完定量统计：

| | 污染率 | 细节 |
|---|---|---|
| **全池** | **76.5%**（剔 289/378） | 直答满分 或 逐字背参考解 |
| MBPP+ | 74.3% | 其中**逐字背出参考解 24.9%**（短函数好背） |
| HumanEval+ | 81.0% | 其中直答满分 71%（教科书解法背得熟，但长代码风格方差大、逐字 dup 率低） |

也就是说，**Qwen3-4B-Instruct-2507 对这两个最经典的代码 benchmark，大约四分之三的题目是"背过的"**。
任何在这两个 benchmark 上报"提分"的工作，都得先回答：你涨的分里，有多少是模型原本就背着的？

我的处理不是回避，而是**把它变成一个可报告的实证 + 一道数据清洗工序**：
- **判 0 门**：归一化逐字相同 → r_pass 直接置 0（背出来的不算分）；
- **背题筛选**：把"直答就满分"和"逐字 dup"的题**全部剔出训练池**——378 题筛完只剩 **89 道净题**。
  这 89 题全是基座直答拿不到满分的（48 题直答甚至 0 分），才是真正能体现"修码能力增量"的题。

温度采样下污染更触目：RFT 阶段我采了 712 条轨迹（89×8），其中 **72 条（每 10 条 1 条）是逐字背参考解**，
全部被过滤、排除在 SFT 数据之外——**背题轨迹绝不进训练数据**，否则污染会扩散。

> 这条线和 Georgia Tech 2026 的一份 reward hacking 实证研究（如实说明：那是本科荣誉论文）呼应——
> 它记录了远程/本地泄题、import 故障注入等真实攻击，且发现"把防御全封死后，训练奖励就涨不上去了"。
> 我的做法一致：**先写防御，再开训练。**

---

## 三、奖励设计与防 hacking 三道门

奖励函数：

```
R = 1.0 * r_pass + 0.2 * r_format − 0.2 * r_over
```

- **r_pass = 隐藏测试逐 case 通过率**（稠密）。用逐 case 而不是整题 0/1，是为了缓解 GRPO 的
  "组内优势全零"——一组采样如果全对或全错，组内归一化后优势=0、没有梯度。稠密奖励让"对了 8/10"
  和"对了 3/10"产生区分度。
- **不上过程奖励**。理论依据是 μCode（ICML 2025, arXiv 2502.20380）的结论：代码生成是"一步可恢复 MDP"——
  任何一个中间代码状态，单轮就能恢复出正确代码，所以单步终端奖励足以支撑多轮，不必上复杂的逐轮信用分配。
- **终止门控**（检索 Agent 项目 run1 用坍缩换来的教训，直接搬过来）：没有合法 `<answer>` 就 r_pass=0。
  不门控的话，"只跑代码不提交"的轨迹能白拿格式分，GRPO 会把策略坍缩成一个无限执行循环
  （检索 Agent 项目实测：不门控时一轮更新作答率从 20% 掉到 0.2%）。

防 reward hacking 三道门：① 禁网断远程泄题；② 隐藏测试只读、永不进 prompt、篡改判 0；
③ 查重门（归一化相同判 0）。

**军备竞赛监控**是重点——我最担心的是模型学会"改写空格/变量名来绕过查重门"。所以每个 checkpoint 我都做
difflib 相似度抽查。iter3 全部 89 条提交 vs 参考解的相似度分布：

- 逐字 dup（sim≈1.0）**6 条**，全部被查重门判 0 ✓；
- near-dup（0.85≤sim<1.0）**40 条**。乍看吓人，但逐条 diff 后发现这是**任务构造的必然后果**——
  buggy 版本本来就是参考解的 AST 单点变异，你把那个变异点改对，输出自然就高度接近原文，这不是"绕查重"。
  抽查三条定性很有意思：一条只差在 generator 外面多套了一层冗余括号（风格差，pass 1.0）；一条少写了
  `min(b+1,10)` 的上界（pass 0.0）；还有一条——模型**"好心"把参考解里的函数名拼写错误
  `lateralsuface` 修正成了 `lateral_surface_area`**，结果测试 harness 调的是原名，直接 0 分
  （过度重构的失败模式，但反过来也证明它不是在背题——背题会原样抄对）；
- 相似度 <0.6 的**独立解法 24 条**——模型并非只会还原参考解。

结论：**4 个 iter 内没有观察到 reward hacking 的演化迹象**（dup 率不升反降、无绕查重改写、无策略性慢代码）。
grade_timeout 我也逐条审读了，都是普通的"算法天然慢"或"上界写错导致大区间",不是故意写慢代码卡判分。

---

## 四、三组对照数字：执行反馈、RFT、多轮 GRPO 各值多少

全部在 89 道净题、贪心解码下跑出（这是交付口径）：

| 模型 | avg_pass | submit | format_ok | avg_runs | dup |
|---|---|---|---|---|---|
| 基座 · 直答（无执行反馈） | 0.2370 | — | — | — | — |
| 基座 · 多轮（带 run_code 反馈） | 0.4648 | 77.5% | 77.5% | 1.27 | 12 |
| RFT · 多轮（93 正例冷启动） | 0.7295 | 88.8% | 88.8% | 1.76 | 8 |
| GRPO iter0 | 0.7305 | 88.8% | 88.8% | 1.82 | 6 |
| **GRPO iter1（峰值）** | **0.7516** | **100%** | **100%** | 1.57 | 6 |
| GRPO iter2 | 0.7436 | 100% | 100% | 1.60 | 6 |
| GRPO iter3 | 0.7435 | 100% | 100% | 1.44 | 6 |

三个增量分别是：

1. **执行反馈的价值：+22.8pt**（0.2370→0.4648，相对提升 96%）。同一个基座，光给它一个能跑代码看报错的
   沙盒，通过率就翻倍。这是多轮修码这个任务"存在性"的依据。
2. **RFT 的价值**：直答 0.2370→0.6952（+45.8pt，2.9×），多轮 0.4648→**0.7295**（+26.5pt）。
   RFT = 温度采样 712 条 → 过滤出 93 条正例（每题≤2，覆盖 53/89 题）→ messages 格式 LoRA SFT。
   但有个关键观察：**RFT 的正例几乎全是"一轮就修对"的（avg_runs=1.03）**——RFT 教会模型"一次写对"，
   却教不会"利用反馈迭代修复"。这正是多轮 RL 的差异化空间，也是"为什么 RFT 不够"的一手证据。
3. **多轮 GRPO 的价值**：贪心峰值 **0.7516（iter1，对 RFT +2.2pt）**，对基座 +27.9pt（相对 60%）。
   最大的单项收益其实是**格式/提交率**——RFT 还遗留 11.2% 的不提交/格式坏，GRPO 第一个完整更新（iter1）
   就把它清到 **100% 并保持**。

GRPO 训练循环完全复用检索 Agent 项目：89 题×G4=356 轨迹/iter、lr 5e-6、KL β 0.05、组内归一化优势、
disable_adapter 零显存参考 KL、零优势过滤、fmt_pct 腰斩熔断（全程未触发）。

---

## 五、最值钱的发现：贪心峰值在 iter1，不在最后一轮

把四个 checkpoint 连成训练进程曲线，会看到一件反直觉的事：

- **训练分布**（temp 1.0 rollout 即时分）avg_pass **单调上升**：0.7065→0.7064→0.7225→0.7354；
- **贪心评测**（temp 0，交付口径）却 **iter1 见顶**：0.7305→**0.7516**→0.7436→0.7435。

**训练分还在涨，部署分已经平了，甚至微微回落。**

原因是：采样口径的奖励里包含了"探索多样性"的收益，而贪心口径只看确定性策略。贪心见顶之后继续训练，
反而把确定性的答案分布弄平。修复轮数也在收敛（1.82→1.57→1.60→1.44）——但**这条"轮数收敛"曲线，
我一开始差点写成了"模型学会修"，深挖之后发现恰恰相反**（见下一节，这是全文我最想讲的一段自我修正）。

**而这件事，和检索 Agent 项目是同一个现象。** 检索 Agent 项目 run3 我也是贪心 iter0（26.60）> iter1 > iter2，
最后按贪心口径选了 iter0 而不是训练更久的 checkpoint。两个完全不同的环境（检索 / 代码），
同一条"训练分↑≠部署分↑"的规律独立复现——这就不是偶然，而是 **GRPO 多轮训练的一个固有性质**。

**还有一个更细的边界，是我做完题级 diff 才敢说出口的。** iter1→iter3 贪心掉了 0.8pt，我把它拆到单题：
平均每题只动 −0.0081，掉分**几乎全部来自一道题**（`c4_he_order_by_points`，iter1 跑 1 次修对满分、
iter3 跑 5 次全败）。n=89 时单题噪声就有 ±1.1pt，比这 0.8pt 的"回落"还大——所以准确的表述是
**"iter1 起进入 0.743~0.752 的平台"，而不是"系统性过拟合回落"**。峰值选点是真的，但别把它讲成
一个比噪声还大的故事。

我的处置纪律也复用了：**按交付口径（贪心）选点 → 部署 iter1**；同时把"两条口径的背离"本身当成一个监控信号。

---

## 六、修复行为深挖：我推翻了"模型学会修"这个叙事——包括我自己差点踩的一个统计伪影

前面表格里有个诱人的读法："修复轮数单调收敛 = 模型学会了修 bug"。**我写了个离线脚本
（`c4_deepdive.py`）拿现有评测日志验证它，结果把这个叙事推翻了一半。**

### Phase 1：纯日志统计——"一次修对率"升了，"修复成功率"没了

把每条轨迹按 `<run_code>` 的执行结果序列拆开：

- **一次修对率**（跑 1 次且隐藏测试满分）：基座 29.2% → RFT 61.8% → GRPO iter1 **65.2%**；
- **修复成功率**（首跑失败后、靠反馈把终版救回满分）：基座 11.1% → RFT 11.8% → iter0 5.9% →
  **iter1-3 全是 0%**（15~18 题的小样本，但方向跨模型一致）。

结论：**轮数收敛是"一次修对率上升 → 修复需求下降"的副产品，不是修复能力上升。** 训练收益的真实形态是
"第一版就写对 + 跑一次验证 + 格式 100%"，而"失败后多轮救回"这条路在 GRPO 之后反而消失了。

### Phase 2：配对判分——基座在"乱改"上丢 9.3pt，GRPO 把这个通道灭绝到 0

光看比例不够，我把每条轨迹**首跑的代码**抠出来，用同一套 `grade_solution` 沙盒离线重判分，
和终版分做配对差（Δ = 终版 − 首跑），再按首跑可见测试结果分支：

| 模型 | 全量 Δ | P 分支 Δ（首跑已过可见测试） | F 分支 Δ（首跑失败） | 首跑 gated 分 |
|---|---|---|---|---|
| 基座多轮 | −0.0599 | **−0.0926**（n=54） | +0.0382（n=18） | 0.6344 |
| RFT 多轮 | +0.0017 | −0.0139 | +0.0676 | 0.7278 |
| GRPO iter1 | **+0.0004** | **0.0**（n=74） | +0.0022 | **0.7512** |
| GRPO iter3 | 0.0 | 0.0 | 0.0 | 0.7435 |

三个能站住的发现：

1. **过度编辑是基座的真实损失通道**：首跑已经过了可见测试的题，基座平均还要继续改，隐藏口径上
   **丢 9.3pt**。GRPO 把它收敛到精确的 **0.0**——模型学会的是"跑一次验证、过了就原样提交"。
2. **训练收益大头在首版质量**：首跑配对口径基座 0.6344 → iter1 0.7512（**+11.7pt**），
   终版分的提升基本全部由首版提升解释，多轮修复环节贡献 ≈0。
3. **F 分支才是全部 headroom**：首跑失败的 15~18 题，所有模型终版都只有 0.07~0.17。
   基座/RFT 的修复增量（+3.8/+6.8pt）真实存在但绝对值太低；GRPO 连这点增量也没了。

### 一个差点写进博客的统计伪影（这一段是全文最重要的方法论教训）

Phase 2 第一版跑出来，所有模型的 Δ 都是负的（−0.056~−0.102），我差点得出"多轮修复净有害"的结论。
逐行检查后发现：**终版分施了"与参考解逐字相同判 0"的 dup 门，首跑分没施**——iter1 有 6 条轨迹
终版 dup 判 0、首跑代码却按原分计，光这 6 条就虚增了 −0.054，几乎解释全部负值。
修成对称门控（首跑代码同样过 dup 判 0）后，Δ 立刻变成上面表里的样子。

教训一句话：**配对比较的两侧，口径必须对称——门控、归一化、判分函数，一个都不能只施一边。**

### 为什么 RL 学不到"修复"？

机理其实 GRPO 自己就说了：难题组内 G=4 全败 → 组内优势全零 → 被过滤，**修复行为根本拿不到梯度信号**。
这正是 μCode"一步可恢复 MDP"（arXiv 2502.20380）的实证注脚：一步可恢复的任务上，
"一次做对"就是奖励地形的最优路径。要让修复行为涌现，得补三样东西——
**多 case 可见反馈**（现在单 assert 太弱，模型看不到自己改坏了哪些 case）、
**保持型奖励**（终版 vs 首跑对比、显式惩罚"改坏已通过"）、**回溯信用分配**（MURPHY 那条线）。
（三天后的续篇里，第三条路径——turn 级信用分配——动手做完了，这条"学不到修复"的结论被部分推翻：见第九节。）

### 检索 Agent 项目镜像：收益形态由任务结构决定

同一天我用同样方法重算了检索 Agent 项目的 500 题贪心日志：多跳 QA 的"一搜即对率"是 **0%**（任务天然必须多步），
RL 的收益形态是**多步行为内的终答质量**——多搜轨迹 EM 从 SFT-only 的 1.40% 升到 **26.71%**，
同时平均搜索 3.96→2.65 收敛到 2-hop 下界。两个环境拼起来是一句话：

**RL 收益形态由任务结构决定：天然多步的任务，RL 提升多步内的质量；一步可恢复的任务，
RL 最大化一次成功率。** "什么时候多轮 RL 有增量"——这是我用两个自研环境换来的第一手回答。

---

## 七、回到缘起：训练层零改动，环境像插件一样换掉了

代码 Agent 项目的 `c4_train.py`，和检索 Agent 项目的 `m3_train.py` 放一起 diff，你会发现**训练循环本体几乎逐行相同**——
组内归一化优势、disable_adapter 参考 KL、零优势过滤、熔断、逐轮落盘，全是从检索 Agent 项目 import 或照搬。
真正改动的只有两处：

1. **环境接口**：`m3_rollout`（检索）→ `c4_rollout`（代码沙盒），多传一个 hidden 判分表；
2. **统计语义**：`r_answer`/`n_search` → `r_pass`/`n_runs`/`dup`。

GRPO 的优势计算函数 `grpo_advantages` 我直接 `from m3_env import`——它本来就是环境无关的。

所以缘起那个问题有了答案：**这套多轮可验证奖励 GRPO，是一个通用范式，环境确实是可插拔的组件。**
这不是嘴上说说，是两个环境、一套训练层、零改动复用跑出来的代码级证据。

---

## 八、诚实的边界

- **单卡小规模**：89 题训练池、4B 模型，GRPO 对 RFT 的 +2.2pt 增量**没有进到文献的 +5pt"优秀"线**
  （89 题信号偏稀，零优势组占比一路升高，有效轨迹 140→100）。
- **不是 SWE-bench**：任务是函数级修 bug，不是真实仓库的多文件题。算力边界如实说——生态基线是
  8×B200 / 16–64 节点 H100 起步，我是 8×V100(sm_70)，所以我做的是"轻量可验证环境 + 防作弊 + 跨环境迁移"，
  不刷 SWE-bench 绝对分。
- **污染筛选是被动防御**：我筛掉了训练池里背过的题，但池外的新题模型仍可能背过。

下一步想做的：LiveCodeBench（训练截止后的新题）扩池、多点组合变异让背过的题重新"不可背"、
8B stretch、turn-level 优势估计（GiGPO/Tree-GRPO 那条线）**（✅ 三天后做完了，见第九节）**、
以及用"早停 iter1 / 降 lr / 升 KL β"去抑制过冲、突破 0.75 这个平台。

---

## 九、续篇：把"下一步"清单做掉的两项

上面那份"下一步想做的"清单，写完三天内我做掉了两项：turn 级信用分配（GiGPO）和推理引擎深度定制（vLLM）。
一项是接着第六节"为什么 RL 学不到修复"往下打的，一项是被"你这套栈怎么放大"这个问题逼出来的。
都按同一套纪律做：判据预注册、单变量、负结果如实写。

### 9.1 turn 级信用分配：先做 0 GPU 的反事实，再花 GPU

第六节的结论是"修复行为拿不到梯度信号"。三条候选路径（多 case 可见反馈、保持型奖励、回溯信用分配）里，
我选了**信用分配**先动手——因为它的假设可以被离线验证，不用先烧卡。

**Stage 0：在历史 rollout 落盘上离线重算 turn 级优势（零 GPU）。** 这一步改写了整个实验的假设，
三个发现：

1. **"GiGPO 给全败组新增信号"在数学上不可能。** 我最初的想法和很多人一样：难题组内 G=4 全败、
   组内优势全零被过滤，那按 turn 分组（锚点分组）是不是就能解锁这些组？离线一算就发现不行——
   同分组内奖励无方差，锚点子组继承的还是零方差（实测"episode 优势为 0 但 turn 优势非 0"的 turn
   数量 = 0，一个都没有）。**真实机理是另一回事：mixed 组内的信用重分配**——53.8% 的非零优势
   turn 的优势值被实质改变，把"摊平到整条轨迹"的信用集中到真正起作用的 turn 上。
2. **修复行为在采样层是存在的。** mixed 组 59 条首跑失败轨迹里 18 条（31%）可见修复（temp 1.0）。
   贪心修复率 0% 是"贪心策略不选"，不是"不会修"——这直接给了 RL 一条可放大的火种。
3. 零优势过滤滤掉的主要是**全对饱和组**（54 个同分组里 43 个 reward=1.2），它本来就该滤。

于是预注册假设从"解锁全败组"改写为"**信用锐化 → 贪心修复率 > 0%**"。顺手还修掉一个设计错误：
GiGPO 原文的折扣 γ<1，但我实测 γ<1 会在同分组里混进"更短更好"的长度效率信号（iter0 有 115 个
turn 凭短拿分）——那是第二个机制，污染单机制归因。改成 γ=1.0：纯信用重分配，零新增信号。

**Stage 1.5：花 1/5 的钱先探方向（配对 replay）。** 同 seed、同 192 样本子集，双臂各自重放训练
一个 iter 再贪心评测——这是能做到的最干净单变量。结果 R1(gigpo) − R0(epi) = **0.0000**，
贪心 89 题 0 翻转。这个 0 怎么解读很关键：按预注册记为 **GATE-PASS-NO-SIGNAL**——方向门（Δ≥0）
过了，进 Stage 2；但单 iter 弱更新（lr 5e-6 × 192 样本）本来就不足以产生贪心行为分化，
**对机制是否起效零宣称**。把"没坏"和"有效"分开记账，是这类实验最容易糊掉的地方。

**Stage 2：全量 4 iters，预注册主判据 PASS。**

| 贪心 avg_pass | iter0 | iter1 | iter2 | iter3 |
|---|---|---|---|---|
| epi 基线（episode 级优势，历史） | 0.7305 | 0.7516 | 0.7436 | 0.7435 |
| gigpo（turn 级优势，本轮） | 0.7182 | **0.7561** | **0.7555** | 0.7200 |

主判据"任一 iter ≥ 0.7526"（历史峰值 + 容差）：**iter1/iter2 双点过线**，基线峰值 0.7516 只是单点。
更想讲的是行为侧：**贪心修复成功率从基线四 iter 全 0 → gigpo iter0 2/17、iter1 1/15**；
失败分支隐藏口径修复增量 iter0 +0.0688，是 episode 基线同口径的 **34×**；基线从未修好的顽固题
（c4_he_order_by_points，epi 卡 0.073）gigpo 用 5-run 深度修复连续拿满分——多轮修复轨迹在
episode 级优势下被信用摊平，正是 turn 级锐化的靶心。

**但这一节的"诚实边界"比结果本身长：** ① 峰值差 +0.0045 在单 run 噪声量级（Stage 1.5 replay
的保真度差 −0.0115 可作参照）——我宣称的是"预注册过线 + 三方向协同证据（双 iter 过线 /
修复率 0→>0 / F 分支 34×）"，**不宣称大效应**；② 修复通道的证据集中在 iter0（iter1 首跑质量更高、
F 分支增量转负）；③ iter3 回落 0.7200，3 道掉分题全部是查重门判 0 的背题漂移——不是能力退化，
但 turn 级信用与背题倾向的关系，单 run 说不了。

回头看第六节的结论，修正了一半：**"失败后迭代修复"不是 RL 学不会，是 episode 级优势教不会**——
换信用分配粒度后，采样层本来就存在的修复行为（18/59）开始向贪心策略迁移。这是我对
"信用分配粒度 × 任务结构"的第一手因果证据，也是 μCode"一步可恢复"边界条件的补充：
一步可恢复任务上 episode 级优势会自然收敛到"一次做对"，但修复通道并没有消失，只是需要更细的信用才够得着。

### 9.2 推理引擎深度定制：一次预注册的提速实验

第二个"下一步"来自一个自然的追问："你这套栈怎么放大？"——我发现自己拿不出实测数字，
只有"verl 在 V100 上不可用所以自研"的防守答案。于是花了两天把 vLLM 在这个负载上摸了一遍。

**头条：CUDA graph 在 V100（sm_70）上 −35.5%，且输出零翻转。** 老卡默认不敢开 CUDA graph
（`enforce_eager=True` 是惯性配置）。六配置矩阵实测，89 题交付负载 generate wall
90.3s → 58.3s，聚合吞吐 290→443 tok/s。但比提速更值钱的是**预注册不变性判据**：
贪心逐题 pass_rate、答案归一化文本全配置对比，5 个配置全部 **0/89 翻转**——防"提速了但输出变了"
的假优化。推理引擎的任何"优化"，不变性核对应该是前置条件，不是事后补丁。

**两个多数人不知道的 prefix caching 事实。** ① vLLM V1 引擎 prefix caching **默认开启**——
"我没配置"不等于"它没开"，做对照实验必须显式 OFF 才成立；② 多轮会话的收益上限由
**KV 容量 vs 并发前缀总量**决定：本负载 KV 池 101k tokens，89 个会话 × ~2.5k ≈ 222k——
轮间前缀被逐出，命中率打折。实测小并发贪心负载 ON/OFF 只差 1.2%（decode 主导稀释了 prefill 收益），
但这条容量账在扩到数百并发时就是第一性约束。**容量规划（并发 × 会话长 ≤ KV 预算）是
多轮 rollout 放大的第一个要算清的数。**

**共置分相：79 : 21，和一个反直觉的拆分。** 对训练 iter 全环分相计时：rollout 相
（引擎+采样+沙盒+换载+merge）占 **78.8%**，训练相只占 21.2%。反直觉的地方在于换载税——
"vLLM 和 HF 交替独占显存要反复重建引擎"听起来就该是大头，实测本体只占 ~2-3%。
**真正的瓶颈不是换载，是 rollout 相和训练相串行无法重叠。** 这个结论直接指向放大方案的正确姿势：
不是换更大的卡，是架构换形（rollout/训练分卡 disaggregated）。

**附赠一个 17 小时的死锁排查记（本轮最好的故事素材）。** 训练循环内建 vLLM 引擎 4/4 挂死在
"Using V2 Model Runner" 之后——权重加载前的父子握手阶段，挂 17 小时不恢复；同镜像下评测 CLI、
replay、裸 `LLM()` 全部正常。全新容器 + 110s 超时的二分实验 E1-E12，逐个 import 组合测试，
最后定位：vLLM V1 的 EngineCore 子进程默认 fork 启动，父进程带着 torch/transformers/peft
加自研栈的线程态去 fork——经典 fork-while-threaded 死锁，**模块组合才触发，单成分全不触发**
（这就是它挑环境的原因）。修复一行环境变量：`VLLM_WORKER_MULTIPROC_METHOD=spawn`。
教训两条：容器挂死先看活体早期信号（引擎 init 一分钟内应有 Loading model，死锁既无 OOM 也无
traceback，别傻等）；自研栈把引擎放进训练进程，就要为进程模型买单。

这些数字最后都汇进了一份 `SCALE_UP.md`（规模放大方案）：实测锚点表、verl 组件级迁移映射
（自研栈每个组件对到 verl 的哪个位置，约 1.5-2 周可迁——"为什么不用 verl"从防守答案变成进攻答案）、
8B → 30B-A3B MoE → 70B 三档外推、千卡 disaggregated 预案。每个数字标 [实测]/[估算]/[文献]——
放大叙事最常见的坑是拿文献数字冒充实测，分级标注是给自己的约束。

---

## 文献坐标

我是这条线在低算力下的独立实践者：RLTF（arXiv 2307.04349，测试反馈在线 RL 开山）→ CodeRL →
μCode（arXiv 2502.20380，一步可恢复 MDP，ICML 2025）→ MURPHY（arXiv 2511.07833，多轮 GRPO+回溯信用分配，
口径"相对最强先前多轮执行反馈方法最高 +6pt"）→ 小模型 RLVR（arXiv 2605.30478，pass@1 最高 +13pt）；
turn 级信用分配对标 GiGPO（arXiv 2505.10978，NeurIPS 2025，episode+锚点分组 step 双层优势——
第九节的锚点重定义为本环境可见判定前缀、γ 修订为 1.0 是两处环境适配）与 Tree-GRPO
（arXiv 2509.21240，需 rollout 引擎支持状态分叉，锁步引擎改造量大一个量级故未选）；
去 Docker 化沙盒对标 SWE-MiniSandbox（arXiv 2602.11210，ICML 2026）。

代码、实验日志（含 10 条完整坑记录）、复现命令都在仓库：[agentic-rl](https://github.com/chenliang19912/agentic-rl)。欢迎拍砖。
