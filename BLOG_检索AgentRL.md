# 单卡 V100 从零自研多轮 GRPO：把 Qwen3-4B 训成"检索—推理—再检索"Agent——EM 4.2→26.6 背后的三次行为坍缩

> 自研轻量 Agentic RL 训练栈 · 检索环境篇 · 完整复盘
> 所有数字真实跑出，口径见仓库 `M0_README.md`。代码 + 实验日志已开源（MIT）。

## 缘起：把 Search-R1 那套范式，从头自己搭一遍

2026 年，Agentic RL——多步环境交互、工具调用、以可验证奖励驱动的强化学习——是大模型后训练最热的主线之一。检索这条线上，Search-R1 系工作把「生成 `<search>` → 检索 → 读结果续生成 → `<answer>` 收手」的多轮 rollout 交给 GRPO 去训，让模型自己学会三件事：**什么时候搜、搜什么、什么时候停止搜索开始作答**。

读论文是一回事，从头搭一遍是另一回事。我想回答几个读论文回答不了的问题：

1. 多轮工具调用的 RL 训练，工程上到底难在哪？（传闻"不稳定"，具体怎么个不稳定法？）
2. "行为靠 RL、知识靠检索"这个范式结论，在自己的栈上独立复现，成不成立？
3. 小参数模型（我手上是 Qwen3-4B）+ 单卡级算力，这套范式的下限和上限各在哪？

硬件起点是一台 8×V100-32GB（sm_70）。这个起点直接决定了技术路线：**主流 RL 训练框架 verl 的官方镜像在 sm_70 上不可用**（torch 2.11 + cu130 预编译不含 sm_70 kernel，任何算子都报 `no kernel image available`；flash-attn 也是 sm_80+ 编译）。与其陷入源码编译的版本地狱，不如**自研一套轻量的多轮 GRPO 训练栈**——反正多轮 rollout + 自定义环境回复格式这些需求，本来就要自己写不少东西，自研反而对每个环节有完整掌控。

结果这条线最终跑出：HotpotQA 多跳问答 EM **4.20 → 26.60**（对同检索器单次真实 RAG +12.8pp，距 oracle 上界仅 3.4pp），RL 消融净增量 +21.2pp，OOD 数据集 Bamboogle EM 20.0。但真正值钱的是过程：**三次行为坍缩/振荡的完整诊断链**、一个与前沿论文方向相反的 REINFORCE 对照结果、以及一次跨语言迁移中"语料覆盖是硬约束"的定量实测。下面按时间线讲。

---

## 一、先算清楚账：基线阶梯标定

动手训任何东西之前，先把 headroom 算清楚。协议：HotpotQA dev(distractor) bridge 题型，n=500（seed=42），Qwen3-4B base，fp16，`/no_think` 关思考 + 简洁短答指令，官方 EM/F1。

| 基线 | EM | F1 | 含义 |
|---|---|---|---|
| A · 无检索直接答 | **4.20** | 13.15 | 下界：纯参数化知识 |
| C · BM25 单次检索 k=6 | **13.80** | 27.31 | 真实 RAG（gold 段落 recall@6 仅 0.371） |
| B · oracle gold 上下文 | **30.00** | 51.86 | 上界参照：白给 2 个 gold 段落 |

三个数字各有各的信息量：

- **A 低到 4.2**：多跳题靠模型自身知识基本无解——检索是刚需，不是锦上添花；
- **C 到 B 之间还有 ~16pp EM**：BM25 单次查询只找回 40% 的 gold 段落，**35.8% 的题一段都找不到**（k=6 时全不中 37.8%）——"换个 query 再搜一次"这个行为本身有真实的价值空间，这正是 RL 要训练的目标行为；
- **B 也只有 30**：base 模型即使拿到完美检索，阅读理解也远未满分——说明即使检索修到满分，模型侧仍有大量空间。

检索环境用 Search-R1 官方语料 wiki-18（**2101.5 万维基段落**），自建 BM25 CSR 倒排索引：14.95 亿 postings、词表 616 万、构建 55 分钟。训练环境统一 TOP_K=6——**基线和训练环境同参**，这样后面所有对比才干净。

---

## 二、SFT 冷启动：两次失败教会我"够用就行"

RL 之前需要 SFT 冷启动，把 `<search>`/`<answer>` 的输出格式和行为种子塞进模型。这里我连续失败了两版，每一版的失败根因都值得写下来。

### v1 教师轨迹版：作答率 4.4%，但藏着一个惊喜

5000 条轨迹（50% 单跳 / 50% 教师两跳，用 Qwen3-4B-Instruct-2507 生成第二跳 query），LoRA 训练。评测（n=500，k=6）：**贪心格式合格率仅 4.4%，平均搜索 3.96 次然后不答**；温度 1.0 下好一些，19.75%。

惊喜是：模型**自发学会了多跳查询改写**——面对没见过的题，会先搜实体 A，再从结果里抽出具体属性构造第二跳 query（"Who played Maria Claudia in La Heredera?" → "Brittany Snow birth date"）。这个行为不在训练数据里，**行为泛化真实存在**。

失败根因定量在数据上：**80% 的训练轨迹是"无证据作答"**——gold 段落召回只有 20%，轨迹却照样教 `<answer>gold`。模型学到的规律是"答案与检索证据无关"，终止行为根本没建立起来。

### v2 证据落地版：更严谨的数据，更彻底的失败

针对 v1 根因重构数据：4059 条，**答案 100% 有证据支撑**（每条轨迹的 `<answer>` 前都有 gold 段落落在检索结果里）。结果：**贪心与温度 1.0 双双 format 0%**，全部轨迹搜索 4 轮不答——比 v1 更差。

根因：3 个 epoch 在"搜索模式占多数"的数据上，把 `<search>` 行为强化成了绝对先验，`<answer>` 终止行为被淹没。一句话总结这次失败：

> **证据落地修的是"答案质量"，不修"何时收手"的分支决策分布。**

### 决策：回退 v1 作为 RL 起点

v1 在温度 1.0 下有 ~20% 作答率——这就是 RL 需要的全部火种。**SFT 冷启动的目标不是完美，是"给 RL 留一个可放大的行为种子"**：终止行为正是 RL 奖励要塑形的东西，SFT 阶段硬修反而会过拟合出搜索循环。后来中文线的四层诊断会更深入地复现并解剖这个现象（见第七节）。

---

## 三、训练栈：verl 不可用之后，1200 行自研多轮 GRPO

自研栈的完整循环，一图流（文字版）：

```
┌─ rollout 相（vLLM，批量多轮，锁步）────────────────┐
│ 128 题 × G=4 → 512 条轨迹                          │
│ 每条轨迹：生成 → 解析 <search> → BM25 检索 k=6 →     │
│           结果拼回上下文 → 续生成 → … → <answer>/超轮 │
├─ 奖励计算 ─────────────────────────────────────────┤
│ R = 1.0×F1(答案) + 0.2×格式 + 0.15×检索命中(门控)    │
│     − 0.2×过度检索                                  │
├─ 训练相（HF + LoRA）────────────────────────────────┤
│ 组内归一化优势 → loss = −adv·logp + β·KL            │
│ 参考模型 = disable_adapter 的冻结 base（零额外显存）  │
├─ merge → 权重回写 vLLM → 下一轮 ────────────────────┤
└─────────────────────────────────────────────────────┘
```

几个关键设计决策：

1. **参考模型零显存实现**：LoRA 场景下，参考模型就是"合上 adapter 的 base"——HF 的 `disable_adapter` 上下文管理器直接复用同一份权重，不需要第二份模型拷贝。单卡 32GB 显存下这是关键。
2. **锁步（lockstep）多轮 rollout**：一批 query 同时发多轮，全部轨迹同步推进，轮末统一截断收 batch。实现简单、日志干净；代价是长尾等待（这个问题后来在规模化方案里用实测数字定量拆解了）。
3. **轨迹级 token mask 的三个坑**（Qwen3 模板）：官方 `return_assistant_tokens_mask` 返回全 0（模板没有 `{% generation %}` 标记）；前缀长度差分法也不可用（模板会按上下文注入空 `<think></think>` 块，前缀渲染与全量渲染不一致）；最后用**字符级锚点 + offset_mapping** 解决——先实测确认"裸 tokenize 文本"与"模板渲染后的 ids"逐位一致，再按字符区间映射回 token mask。
4. **稳定性基建先于训练**：每轮 rollout 落盘 `iter{N}_rollouts.jsonl`（崩溃可复盘的前提）、作答率熔断（关键行为指标腰斩即停，保住最后一个正常 checkpoint）、零优势轨迹跳过（省算力，也是 DAPO 动态采样思想的同源实现——全组同分的样本没有梯度信息，留着只会稀释）。

---

## 四、奖励设计与三次行为坍缩——本文最想讲的部分

### 坍缩一（run1）：过程奖励不门控，一轮更新作答行为灭绝

| 迭代 | avg_reward | avg_f1 | 作答率 | 平均搜索 |
|---|---|---|---|---|
| ITER 0（SFT 起点） | 0.139 | 0.099 | 20.1% | 3.80 |
| ITER 1（一轮更新后） | **−0.030** | **0.000** | **0.2%** | 3.99 |

只做了一轮策略更新，作答行为几乎灭绝，全部轨迹退化为"搜满 4 轮不答"。逐轮落盘的日志给出了干净的归因：

**根因：过程奖励（检索命中 gold 段 +0.15）对"搜而不答"的轨迹照样发放。** 全不答组（占 80%）内部奖励方差完全来自检索命中——GRPO 是方差驱动的，梯度全部集中在"多搜、搜得更准"上；"终止作答"的信号只存在于 20% 的混合组里，被稀释到看不见。最有说服力的数据佐证：一轮更新后**检索命中率 0.40→0.47、平均搜索 3.80→3.99**——模型不是"学坏了"，它是**精确地朝着奖励方差最大的方向收敛**，作答行为不是被压制，是没有任何力量维持它。

这是 reward hacking 在过程奖励上的变体：**"过程"本身变成了比"结果"更容易拿的分**。回头看 Search-R1 只用"答案正确性 + 格式"两类奖励、不给检索中间奖励，正是同一考虑。

**修复**：`compute_reward(gate_process=True)`——轨迹未以合法 `<answer>` 终止时，过程奖励一律置 0。门控后全不答组 reward 全 0 → 组内优势全零 → 被零优势过滤跳过，梯度只来自"答/不答"的组间对照。配套：KL β 0.004→0.02、LoRA lr 2e-5→1e-5。

> 一句话教训：**模型优化的是你写下来的奖励函数，不是你心里想要的行为。** 多轮 agentic RL 里，中间过程奖励必须由合法终止门控。

### 振荡二（run2）：一轮塑形成功，第二轮过冲荡回

| 迭代 | avg_reward | avg_f1 | 作答率 | 平均搜索 |
|---|---|---|---|---|
| ITER 0 | 0.0785 | 0.099 | 20.1% | 3.80 |
| ITER 1 | **0.503** | **0.281** | **86.1%** | 3.04 |
| ITER 2 | 0.032 | 0.067 | **14.3%** | 3.71 |

门控修复立竿见影：**一轮更新即完成行为塑形**——作答率 20.1%→86.1%，F1 翻近三倍，且模型学会"少搜早答"（3.80→3.04）。这印证了核心假设：终止行为本来就是 SFT 起点里被淹没的少数模式，干净的组对比梯度一轮就能把它放大成主流。

然后 ITER 2 荡回 14.3%——**20→86→14 的三拍振荡**。熔断器按设计动作，保住了 iter0/iter1。

振荡机理值得细讲：ITER 1 的训练信号里 86% 的组是"全答组"，组内优势由 **F1 的细差**主导；大量低 F1 的作答轨迹拿到负优势，而 RL 的抑制是**整条轨迹**的——负样本与正样本共享绝大部分结构（搜索、阅读、格式全一样，只有终止动作稀有）时，稀疏奖励会把"终止"这个稀有动作连同失败一起冲掉。叠加单轮 90 个优化步（单批 on-policy 数据上步数过多）与 β=0.02 的弱锚，单轮即过冲。

> 教训：**更新强度（lr × 步数）必须随成功率上升而下调**——成功率越高，组内对比越细碎，越要温和。

### run3：温和超参，单调收敛

从 run2-iter0 继续，lr 5e-6、β 0.05、每轮 ≤24 优化步。四轮全程无熔断、无振荡，贪心复评 iter0 即峰值（见第六节的口径讨论），**run3-iter0 定为最终模型**。

三次坍缩/振荡全部被"逐轮落盘 + 行为指标监控 + 熔断"接住、并在当天内定位修复——我认为这套稳定性基建比主结果数字更有复用价值。

---

## 五、REINFORCE 对照：一个与前沿论文方向相反的结果

Search-R1++（arXiv 2602.19526）在多轮搜索场景系统比较了 REINFORCE/PPO/GRPO，结论是"REINFORCE 最终性能与效率最好、GRPO 稳定性最差"。我用同条件对照检验了这个结论在我的设置下是否成立。

**设置**：同一起点（v1 SFT merged）、同一数据切片（128 题 × G=4）、同一超参（lr 1e-5、β 0.02、温度 1.0），唯一变量是优势估计——REINFORCE 用 `A = r − batch_mean`（全局基线，无组结构），GRPO 用组内归一化。门控、熔断、落盘全部保留。

| 迭代 | REINFORCE 作答率 | GRPO 作答率 |
|---|---|---|
| ITER 0 | 15.8% | 20.1% |
| ITER 1 | **2.3% → 熔断** | **86.1%（塑形成功）** |

REINFORCE 唯一 checkpoint 贪心复评：EM **0.20**、format 1.0%、平均搜索 3.98——比 SFT 起点（1.40）还差，行为形态是与 run1 同款的搜索循环。

**机理**：起点作答率只有 ~16% 时，奖励分布是"大量 0 + 少量正"——`A = r − mean`（mean≈0.043）让 **84% 的未答轨迹全部拿到小负优势（≈−0.04）**。它们数量占绝对多数，负梯度的体量与少数正梯度（作答轨迹 +0.2~0.6）相当甚至更大，更新方向变成"全面压制"而非"放大作答"。而 GRPO 的组内归一化天然是一门课程：**全不答组优势恒为零、被零优势过滤，梯度只来自有作答对照的组**——方向纯净地放大少数派行为。

**与论文的关系（诚实表述）**：我们的方向与论文相反——在**低起点作答率、小批量（128 题/轮）、单卡小规模**的设置下，REINFORCE 比 GRPO 坍缩得更快。两边互证的是那个更大的结论："多轮场景下优势估计极不稳定"（GRPO 三拍振荡 / REINFORCE 一轮坍缩，发生在同一起点上）。论文"REINFORCE 最好"基于其高起点、大数据量、长训练的设置；我把自己的结论限定在实测范围内，不泛化引用。

---

## 六、主结果：行为增量完全来自 RL

### 结果阶梯（HotpotQA dev bridge n=500，贪心，agentic 多轮协议）

| 配置 | EM | F1 | 作答率 | 平均搜索 |
|---|---|---|---|---|
| base 无检索 | 4.20 | 13.15 | — | — |
| BM25 k=6 单次真实 RAG | 13.80 | 27.31 | — | — |
| SFT 冷启动 only | 1.40 | 1.66 | 4.4% | 3.96 |
| **SFT + 多轮 GRPO（最终）** | **26.60** | **37.04** | **99.6%** | **2.65** |
| oracle gold 上下文（上界） | 30.00 | 51.86 | — | — |

三组控制变量把增量归因钉死：

1. **检索器不动**：最终模型与 RAG 基线同索引、同 k=6——对真实 RAG 的 **+12.8pp EM** 全部来自行为（多轮再检索 vs 单次检索）；
2. **冷启动不动**：同一 SFT 模型，加 GRPO 前 EM 1.40、加后 26.60，**RL 消融净增量 +21.2pp**——"行为靠 RL、知识靠检索"的 Search-R1 范式结论在本栈独立复现；
3. **数据不重叠**：RL 训练题与评测题严格互斥，OOD 数据集 **Bamboogle（n=125，训练时从未见过）EM 20.00 / F1 29.16 / format 96.8%**——排除背题。

### 行为塑形：学会"少搜早答"

平均搜索次数 **3.96 → 2.65**，逼近 2-hop 任务的搜索次数下界；检索命中过程分随训练单调上升；终止作答率 4.4% → 99.6%。模型不是搜得更多，是**搜得更准、收手更果断**。

### 贪心/采样口径背离：最终模型为什么选"训练最少"的 checkpoint

run3 各 checkpoint 贪心复评：**iter0（26.60）> iter1（26.20）> iter2（23.80）**；而同期温度 1.0 训练口径的 reward 和 F1 还在单调上升（rollout F1 0.336→0.406→0.434）。

**训练分还在涨，部署分已经见顶回落。** 机理：采样口径的奖励包含探索多样性收益，贪心口径只看确定性策略——贪心见顶后继续训练，反而把确定性的答案分布弄散。

决策纪律：**按交付口径（贪心）选点**，选定 iter0；把"两条口径的背离"本身当作一个监控信号。这个现象后来在代码修码环境上第二次独立复现（贪心峰值出现在 iter1 而非末轮），两个环境同一背离——它不是偶然，是 GRPO 多轮训练的固有性质。姊妹篇博客里有展开。

---

## 七、中文迁移：语料覆盖是硬约束，以及一次四层递进的失败诊断

同一套管线（BM25 检索环境 + SFT 冷启动 + 门控奖励多轮 GRPO）原样迁移到中文：维基中文语料 700.7 万段（字 bigram 分词）、教师模型全量翻译 HotpotQA（train 49,705 / dev 3,843 题，JSON parse_ok 99.2%）、同一套索引与训练代码（只换语料格式适配与中文 system prompt）。

### 先算覆盖，再谈训练

中文维基只有 128.6 万文章（英文 590 万），HotpotQA 实体的中文条目覆盖率很低。锚定（译名 → 语料规范标题）实测：精确锚定率仅 **1.2–1.5%**，加上检索变体锚定（处理"里希特霍芬/李希霍芬"式译名变体）后标题级也只有 **17–18%**。再加证据覆盖、答案证据核验两道门控，最终幸存率 **~15%**（8,578 → 1,293 条训练池），并先构造了一个 135 题的"语料内可答"评测集。

> 方法论教训：**跨语料迁移，先算实体覆盖、先建"语料内可答"的评测集，再谈指标**——否则跑出来的数字没有解释权。

### SFT 连续失败的四层递进诊断

中文线的 SFT 冷启动连续失败（作答率 0%），我做了四层递进排查，每一层都是一次假设→定量验证→证伪/确认：

1. **截断层**：中文多跳轨迹 p95≈3150 字符，沿用英文经验的 `MAX_LEN=2048` 把大量轨迹从中间截断——尾部的 `<answer>` 轮从未进训练。修复（4096 重训）后**仍然 0%**，且定量复查发现截断其实只有 1/775 条——不是主因。
2. **信号密度层**：答案轮中位仅 12 token（占轨迹 ~0.7%），中文数据单跳占比仅 14%（英文 ~1/3），答案行为总曝光只有英文线的 ~1/5。修复（单跳 ×3 再平衡 + 5 epochs）后**仍然 0%**——曝光也不是主因。
3. **条件策略层**：训练数据 100% 带落地证据，模型可能学成了"**见到 gold 证据才作答**"的条件策略。定量探针（`zh_recall_probe.py`）：真实检索 top-10 全中两个 gold 标题仅 **16.3%**——条件几乎永不触发，所以搜索循环。这与英文线 v2 的失败完全同构：**证据落地范式修答案质量，不修分支决策**。
4. **决定性实验——截断前缀续写**：把**训练集原样前缀**截到"搜索结果刚出现"处让模型续写。三组各 20 条，**全部续 `<search>`、0 条产 `<answer>`**——连"训练目标里下一 token 就是 `<answer>`"的前缀也照样续搜索。一刀切开前面所有假设：**不是分布迁移、不是条件不满足，是答案分支根本没学会**（基座在中文域的"继续搜索"先验极强；旁证：loss 卡在 0.3 平台，而五 epoch 对照组能压到 0.116）。

对策：在合并模型上叠新 LoRA 续训加曝光（v5）。火种点燃：loss 0.30→0.133，前缀探针从 0/20 → 5/20 产 `<answer>`，温度 1.0 作答率 45%。中间还埋着一个基建坑：中文轨迹带 `<think>` 推理块，**200 token 生成预算被思考吃光**（96% 轨迹截断在思考中）——放宽到 512 后作答率才显形。"模型不答"先排除生成预算被思考吃掉，别急着归因行为。

> 这一层的通用教训：**"行为分支学没学会"，用训练原样前缀续写一刀切开分布问题与学习问题。**

### 中文线结果：主结论在第二门语言同向复现

| 配置（n=135 门控评测集，字符级指标） | EM | F1 | 作答率 |
|---|---|---|---|
| 无检索直答 | 0.0 | 15.12 | — |
| BM25 单次真实 RAG | 0.0 | 16.17 | — |
| SFT-only（v5 起点） | 5.93 | 14.64 | 42.96% |
| **SFT + 多轮 GRPO（iter1，贪心选型）** | **8.15** | **20.83** | **60.0%** |

+GRPO 后 F1 14.64 → **20.83**（RL 消融 +6.19pp），超同检索器单次 RAG（16.17）**+4.66pp**——"行为增量来自 RL"在第二门语言同向复现。贪心/采样口径背离也在中文线第二次出现（iter2 采样作答率更高、贪心 F1 回落，仍按贪心选 iter1）。

增益幅度小于英文线（+6.19pp vs +21.2pp），恰是**语料覆盖硬约束的定量体现：方法可迁移，上限随语料**。

---

## 八、这套栈后来去了哪：环境是可插拔的

本项目收官后，我把这套训练栈**零改动**迁移到了代码执行环境：`<search>` 换成 `<run_code>`（无容器沙盒）、多跳问答换成 MBPP/HumanEval 注入 bug 的多轮修码，训练循环逐行复用本项目代码——同一套"锁步多轮 GRPO + 组内归一化 + 零优势过滤 + 熔断"在第二个环境跑出了同构的正结果（顺带实测了 Qwen3-4B 对两大代码 benchmark 76.5% 的污染率）。检索/代码两个环境都成立，"可验证奖励多轮 GRPO 是通用范式、环境是可插拔组件"才有代码级证据。详见姊妹篇：**《单卡 V100 上，我把检索 Agent 的训练栈原样搬到代码修 bug 上》**。

---

## 九、诚实边界

- **单卡小规模**：128 题/轮、4B 模型、单卡训练。REINFORCE 对照、振荡等结论限定在此规模，不外推到大规模训练。
- **检索器固定为 BM25**，未与 RL 联合优化；gold recall 0.371 意味着检索仍是硬约束（距 oracle 上界的 3.4pp 里有多少是检索天花板，未拆分）。
- **奖励是规则可验证型**（EM/F1 + 格式 + 检索命中），换到开放域任务需要 RM/LLM-judge，reward hacking 风险换形态。
- **贪心/采样背离只有现象与机理假设，没有受控验证**（未做"贪心口径早停扫描"类实验）。
- 中文线 n=135，小样本；字符级 EM 极严（译法微差即 0），以 F1 为主指标。

---

## 文献坐标

Search-R1（arXiv 2503.09516，本线直接对标的多跳检索 RL 范式）→ ReSearch/DeepRetrieval 系迭代 → Search-R1++（arXiv 2602.19526，多轮搜索场景 REINFORCE/PPO/GRPO 系统对比，本文第五节的对照对象与其结论方向相反）→ DAPO（arXiv 2503.14476，动态采样思想，本文零优势过滤的同源实现）→ GiGPO/Tree-GRPO（turn 级优势估计，本栈后续在代码环境上做了 GiGPO 式改造）。

代码、实验日志（含全部踩坑记录）、复现命令都在仓库：`github.com/chenliang19912/agentic-rl`。欢迎拍砖。
