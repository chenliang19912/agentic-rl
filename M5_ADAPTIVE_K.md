# M5：自适应检索 k——把检索参数变成策略动作（预注册负结果，2026-09-19）

> 检索 Agent 项目（Search-R1 线）的"检索器联合优化"第一步：BM25 top-k 原本固定为 6，
> 本实验把它交给策略设定（`<search k=N>`），检验 RL 在可验证奖励下能否自发学会按题
> 调节检索量。**结论先行：k 动作未涌现，主判据不达标，且发现"动作空间扩展的 prompt
> 成本"——负结果完整报告。**

## 一、协议

- **动作空间**：`<search>query</search>` 依旧合法（默认 6 条）；`<search k=N>query</search>`
  请求 k 条，N 为整数 1..12（环境侧钳制，越界不惩罚——动作空间定义的一部分）。
  k 为可选参数是刻意的防坍缩设计：SFT 冷启动策略从未见过 k 语法，强制新语法有
  run1 式行为坍缩风险；可选设计同时把"k 是否被自发使用"变成可检验的问题。
- **奖励不变**：R = F1 + 0.2·格式 + 0.15·门控过程分 − 0.2·超限（与 run3 完全同权重）。
  k 选择没有专属奖励——它的收益只能通过 F1/过程分间接体现。
- **SYSTEM 只描述动作空间**（k 语法与取值范围），不含任何"建议用大 k/小 k"的引导。
- **配对设计**：与 run3 同冷启动（m3_run2/iter0_merged）、同超参（lr 5e-6、β 0.05、
  128 题×G4、4 iters、temp 1.0）、同 rl_train 数据切片；唯一差异是检索协议。

## 二、预注册判据（训练前落盘）

主判据：贪心 EM（dev bridge n=500）相对 run3-iter0（26.60）**非劣（±1.0pt）且检索
结果字符量下降 ≥10%**；或 EM +≥2.0pt 且搜索次数不升。次级问题：k 指定率与 k 分布——
若趋零/退化为常数即判"未涌现"，如实报告。

## 三、结果（n=500 贪心，同一套判分）

训练照常完成、无坍缩（格式 68.6%→95.1%，avg_search 3.29→2.69），
但 **k 指定率全程 0.0~0.2%：k 动作未涌现**。

| 配置 | SYSTEM | EM (pct_perfect) | 格式 OK | avg_search | 检索字符量 |
|---|---|---|---|---|---|
| run3-iter0（锚点） | plain | **26.60** | ~99 | 2.78* | – |
| **对照组：run3-iter0 冻结** | **M5（提及 k）** | **23.6** | 93.2% | 3.02 | 11603 |
| M5 iter0 | M5 | 23.4 | 91.8% | 3.05 | 11677 |
| M5 iter1 | M5 | 24.4 | – | 2.97 | 11514 |
| M5 iter2（M5 最佳） | M5 | 25.2 | 98.8% | 2.83 | 11106 |
| M5 iter3 | M5 | 24.0 | 94.0% | 3.02 | 11630 |

*run3-iter0 锚点为 m3_rollout 历史口径（检索字符量无同池对照）。

## 四、三条结论

1. **主判据不达标**：M5 最佳 25.2 vs 26.60，非劣（±1.0pt）不成立，EM +2pt 亦不成立。
2. **k 未涌现**：评测 k_specified 0.0%、训练后期 0.1%。策略从未把 k 当作有用动作——
   与代码修码项目"修复行为未涌现"（`C4_README.md` §C.5）同构，是"动作空间扩展 ≠
   行为涌现"的第二例：**没有梯度信号的选项，RL 不会自发去用**（k 的收益只能通过
   F1 间接体现，而多数题在 k=6 下已可答，调节 k 的轨迹与默认轨迹组内难分）。
3. **动作空间扩展的 prompt 成本（本实验最有价值的发现）**：仅在 SYSTEM 描述 k 选项
   （不引导、不强制），就使冻结的 run3 策略掉 3.0pt（26.60→23.6，格式 99%→93.2%）。
   M5 的 RL 训练（23.4→25.2）本质上是追回了这一扰动，而非取得增益。含义：
   **扩展动作空间不是免费的**——对在小动作空间上收敛的策略， merely 描述新选项
   就足以扰动输出分布；新动作要产生正收益，可能需要在冷启动数据中预集成（示例级
   暴露），或给出明确的任务分布理由。

## 五、诚实边界

- n=500 单 run、单规模（4B、单卡、4 iters 预算）；负结果不外推到更大规模、更长训练
  或 SFT 集成 k 示例的变体（A1' 候选后续：在 M2 SFT 数据中混入 k 使用轨迹）。
- 竞赛式判据下 avg_search_chars 主要由搜索次数驱动（k 全为 6）——"检索成本下降 ≥10%"
  的判据实际由搜索次数收敛完成（11805→10512，−11%），与 k 无关，如实说明。
- 未做：k 探索的多 seed 复现；k 连续正确使用的奖励消融。

## 六、产物

- 代码：`m5_env.py`（协议+奖励复用）、`m5_rollout.py`（评测/训练共用引擎）、
  `m5_train.py`（GRPO 循环，逐行复用 m3_train）、`run_m5_evals.sh`（含对照组）
- 训练产物：`checkpoints/m5/`（iter0-3 lora+merged、逐轮 rollouts、stats.jsonl）
