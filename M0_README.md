# 检索 Agent Agentic RL — 实验日志（M0 环境验证 ✅ / M1 基线标定 ✅）

> 成功标准：跑通 1 条多轮"生成 <search> → 检索 → 续生成 → <answer>"的 rollout。
> **结论：达成。且环境比预想好得多——本机是 8×V100 N 卡 + 现成的 V100 定制 vLLM 镜像。**

## 环境实测结论

| 项 | 实测结果 |
|---|---|
| GPU | **8 × Tesla V100-SXM2-32GB（NVIDIA, CUDA 12.4/12.8）**——本机不是 MI308X/ROCm，原计划头号风险消除 |
| 系统 | CentOS 7, glibc 2.17 → **宿主机装不了现代 vllm/torch（manylinux_2_28）**，一切走 Docker |
| Docker | ✅ nvidia runtime 可用；镜像库已有 `1cat-vllm:v100-1.3.0`（py3.12 + torch 2.10 + **vLLM 1.3.0 V100 特化版，内置 SM70 flash-attn 移植**）、`verlai/verl:vllm024.dev2`（后续 GRPO 直接用）、modelscope 系镜像 |
| 模型 | NAS `/mnt/nas3/shared/model/` 现成：**Qwen3-4B / Qwen3-4B-Instruct-2507**（目标基座✅）、Qwen3.5-2B、bge-m3（M1 检索器候选） |
| 网络 | huggingface.co ❌ 被墙；modelscope ✅ / hf-mirror ✅ |
| 速度 | Qwen3-4B fp16 单卡 V100：prefill ~23 tok/s、decode ~26 tok/s（enforce_eager） |

## M0 验证结果

1. ✅ **vLLM 1.3.0 (V100 patch) 加载 Qwen3-4B 成功**，生成正常（`logs/smoke3.log`）
2. ✅ **多轮 rollout 循环跑通**（`m0_rollout.py`）：模型自主发出 `<search>director of Inception</search>` → BM25 返回文档 → 续生成 → 输出，全链路无工程错误。
   ⚠️ 注意：脚本最后打印的 `PASS` 是**子串误判**（答案里恰好含 "London" 字样），实际模型回答"检索结果没有出生地、无法作答"——因为朴素 BM25 第一跳漏掉了含 birthplace 的文档。**工程链路成功 ≠ 任务答对**，这正好是 M1/RL 要解决的。
3. ❌ **Qwen3.5 系（混合线性注意力）在此栈不可用**：`qwen_gdn_linear_attn` 的 tilelang kernel 在 sm_70 编译失败（bf16→half 转换错误）。**选型锁定 Qwen3 系列**（标准注意力）
4. ⚠️ **发现①（印证 SFT 冷启动必要性）**：base Qwen3-4B 不遵守 `<answer>` 标签（直接自由作答），且 `enable_thinking=False` 未完全压住 think 块 → 格式奖励 + SFT 冷启动不是可选项，是必需项
5. ⚠️ **发现②（印证过程奖励/RL 价值）**：朴素 BM25 漏掉第二跳文档时，模型选择"宣布信息不足"而不是**再搜一次** → 多跳搜索行为正是要靠 RL 训练出来的目标行为

## M1 基线标定结果（✅ 已完成 2026-09-01）

**协议**：HotpotQA dev(distractor) bridge 题型，n=500（seed=42），Qwen3-4B base，fp16/enforce_eager，
`/no_think` 关思考 + 简洁短答指令，官方 EM/F1。脚本 `m1_baseline.py`，结果 `logs/m1_baseline_results.json`。

| 基线 | EM | F1 | 含义 |
|---|---|---|---|
| A 无检索直接答（下界） | **4.20** | **13.15** | 纯参数化知识，多跳题几乎不会 |
| B gold 上下文 RAG（上界参照） | **30.00** | **51.86** | 白给 2 个 gold 段落后的阅读理解上限 |

**解读**：
- A→B 之间 **~26pp EM / ~39pp F1 的 headroom**，就是"检索 + 多跳推理训练"可争取的全部空间。A 之低（4.2 EM）证明多跳题靠模型自身知识基本无解，**检索是刚需而非锦上添花**——这是本项目价值主张的第一个实证数字。
- B 只有 30 EM：base 模型（非 Instruct）+ 短答协议下阅读理解也远未满分，说明即使检索完美仍有 70% 提升空间，RL/SFT 有用武之地。
- **发现③（再次印证 SFT 冷启动必要性）**：base 模型对 `/no_think` 指令服从不稳定，输出头部仍回显空 `<think></think>` 块（首次跑因未清洗导致 EM 全 0 的假阴性，已在 `clean_pred` 修复）。格式/指令服从问题贯穿始终 → M2 冷启动不能跳过。
- **踩坑记录**：① Qwen3 tokenizer 的 `enable_thinking` 经 vLLM `apply_chat_template(extra_kwargs=...)` 传入**不生效**，必须走 `/no_think` 系统指令；② 评测必须清洗 think 标签，否则假阴性。

## 基线 C 结果（✅ 已完成 2026-09-02）

**检索环境**：Search-R1 官方语料 `PeterJinGo/wiki-18-corpus`（gz 内是 tar 包，成员 `wiki_dump.jsonl`，
**2101.5 万**维基段落）→ 自建 BM25 CSR 倒排索引（`build_bm25_index.py`，55 分钟；
14.95 亿 postings、vocab 616 万、avgdl 105）。脚本 `m1_baseline_c.py`（含 gold recall@k 诊断）。

**M1 完整基线阶梯**（HotpotQA dev bridge n=500，Qwen3-4B，同一协议）：

| 基线 | EM | F1 | gold 段落 recall | 含义 |
|---|---|---|---|---|
| A 无检索 | 4.20 | 13.15 | — | 下界：参数化知识 |
| **C BM25 单次检索 k=6**（训练环境同参） | **13.80** | **27.31** | 0.371（全中 12%／全不中 37.8%） | 真实 RAG：检索质量约束 |
| C BM25 单次检索 k=10 | 16.40 | 30.07 | 0.407（全中 16%／全不中 34.6%） | 同上，更大 k |
| B oracle gold 上下文 | 30.00 | 51.86 | 1.0 | 上界参照 |

**解读**：
- A→C +12pp EM：检索本身价值确证；C→B 还有 ~14pp EM headroom。
- **BM25 单次查询只找回 40% gold 段落，35% 的题一段都找不到** → "换 query 再搜"（多跳检索行为）
  正是 RL 要训练的核心目标行为，且上限空间真实存在（这是本项目价值主张的最强实证）。
- 决策：**训练环境统一 TOP_K=6**（C@k=6 为 RL 逼近目标）；若 M3 后仍被检索封顶，再上 NAS 现成的 bge-m3 稠密检索。

**数据补齐**（全部来自 `RUC-NLPIR/FlashRAG_datasets`，与 Search-R1 官方同源，口径可对齐）：
Bamboogle test 125 题（OOD）、2Wiki dev/train、HotpotQA train 两分片（M2/M3 用）。

## verl 镜像预研结论（2026-09-01，重要）

❌ **`verlai/verl:vllm024.dev2` 在本机不可用**：内含 torch 2.11+cu130，**不含 sm_70 kernel**，
V100 上任何算子都报 `no kernel image available`（flash_attn 2.8.3 也是 sm_80+ 编译）。
且镜像内无 `verl` 包（需源码安装）、transformers 5.5.3 过新。

**M3 框架决策**：放弃官方 verl 镜像，改为
**`1cat-vllm:v100-1.3.0`（已验证可跑 V100）做 rollout + 自研轻量多轮 GRPO 训练循环**。
理由：硬件约束下唯一确定能跑的组合；自研 rollout/奖励/训练闭环对每个环节有完整掌控与可复现性。

备选：verl 源码 + torch≤2.10 cu128 手工装（版本地狱，时间成本高，不做首选）。

## M2 SFT 冷启动（2026-09-02 进行中，已到 v2）

### v1：教师轨迹版（已训练，评测失败，诊断明确）

- 数据 `data/sft_train.jsonl`：5000 条（50% 单跳 / 50% 教师两跳，Qwen3-4B-Instruct-2507 生成第二跳 query），3 epochs→实际 1 epoch、3 卡 LoRA r=32、45 分钟
- **评测结果（dev bridge n=500，k=6）**：贪心 format 合格率仅 **4.4%**，平均搜索 3.96 次不答；温度 1.0 采样下 format 19.75%
- **惊喜发现**：模型自发学会多跳查询改写（如 "Who played Maria Claudia in La Heredera?"、"Brittany Snow birth date"），且该题不在训练集——行为泛化真实存在
- **失败根因诊断**：
  1. **80% 训练轨迹"无证据作答"**：gold 段落 recall 仅 20%，轨迹却照样教 `<answer>gold`——模型学到答案与证据无关，终止行为没建立
  2. 教师自由生成的第二跳 query 召回太低
  3. 只训 1 epoch，`<answer>` 收尾曝光不足
- **教训**：冷启动数据的**证据链完整性**比数量重要；SFT 教不会的行为要用数据分布修，不是加训时长

### v2：证据落地版（已训练，评测失败——过拟合"搜索循环"）

- 数据 `data/sft_train_v2.jsonl`：4059 条（单跳 1416 / 多跳 2643），答案 100% 有证据支撑
- 训练：3 epochs、3 卡 → `checkpoints/m2_sft_lora_v2`
- **评测结果：贪心与温度 1.0 双双 format 0%、平均搜索 4.0 次、零作答**——比 v1 更差
- **根因**：3 epochs 在"搜索模式占多数"的数据上把 `<search>` 行为强化成绝对先验，
  `<answer>` 终止行为被淹没；证据落地修的是"答案质量"，没修"分支决策分布"
- **结论**：**回退 v1 模型作为 RL 起点**（温度 1.0 下仍有 ~20% 作答率，GRPO 有信号；
  终止行为正是 RL 奖励要塑形的目标）。SFT 冷启动"够用就行"，追求完美反而过拟合

## M3 自研多轮 GRPO（2026-09-02 启动）

- 起点：v1 merged（`checkpoints/m2_sft_merged`）；RL 数据 `data/rl_train.jsonl` 4000 题（与 SFT 严格不重叠）
- 循环：vLLM 批量多轮 rollout（G=4/题，温度 1.0，top_k=6）→ 组内归一化优势 →
  HF+LoRA 策略更新（`loss = -adv·logp + β·KL`，参考模型 = disable_adapter 的冻结 base，
  零额外显存）→ 合并权重 → 下一轮

### run1（已中止——奖励设计缺陷导致行为坍缩，2026-09-02 上午诊断）

| 迭代 | avg_reward | avg_f1 | fmt_pct | avg_search |
|---|---|---|---|---|
| ITER 0（v1 SFT 起点） | 0.139 | 0.099 | 20.1 | 3.80 |
| ITER 1（一轮更新后） | **−0.030** | **0.000** | **0.2** | **3.99** |

**现象**：仅一轮策略更新，作答行为几乎灭绝，全部轨迹退化为"搜满 4 轮不答"。

**根因诊断（三条）**：
1. **过程奖励未门控终止行为（主因）**：R_process（检索命中 gold 段）对"搜而不答"的轨迹
   照样发放。全不答组（占 80%）内奖励方差完全来自检索命中，GRPO 梯度把更新量集中在
   "多搜、搜得更准"上；"终止作答"信号只存在于 20% 的混合组，被稀释。数据佐证：
   一轮更新后检索命中率 0.40→0.47、平均搜索 3.80→3.99——模型精确地朝着奖励方差大的
   方向收敛，作答行为不是被压制，是没有任何力量维持它。
2. **KL 锚太弱**：β=0.004 拉不住策略偏离 SFT 起点（作答行为种子在起点里有 20%）。
3. **单轮步数×学习率过大**：512 条轨迹 128 个优化步、LoRA lr 2e-5，一轮即完成行为切换。

**教训**：多轮 agentic RL 里，**中间过程奖励必须由合法终止门控**，否则
"过程"本身会变成比"结果"更容易拿的分，策略坍缩成刷过程分的循环；这是 reward hacking
在过程奖励上的变体。Search-R1 只用"答案正确性 + 格式"两类奖励、不给检索中间奖励，
现在回头看正是同一考虑。另：每轮落盘 rollout、设置作答率熔断，是崩溃可复盘的前提。

**修复（run2）**：
1. `compute_reward(gate_process=True)`：轨迹未以合法 `<answer>` 终止时 R_process=0
   （全不答组 reward 全 0 → 优势 0 → 不产生梯度；梯度只来自"答/不答"的对照）
2. KL β 0.004→0.02；LoRA lr 2e-5→1e-5
3. 零优势轨迹跳过训练（省算力）；每轮落盘 `iter{N}_rollouts.jsonl`；
   fmt_pct 较上轮腰斩即熔断保住最后正常 checkpoint
4. 从 v1 SFT merged 全新重启（产物归档至 `checkpoints/m3_run1_failed`）

### run2（2026-09-02 启动，GPU 6，日志 `logs/m3_grpo_v2.log`）

| 迭代 | avg_reward | avg_f1 | fmt_pct | avg_search | 有效轨迹/总 |
|---|---|---|---|---|---|
| ITER 0（v1 SFT 起点） | 0.0785 | 0.099 | 20.1 | 3.80 | 260/512 |
| ITER 1 | **0.503** | **0.281** | **86.1** | 3.04 | 362/512 |

- **一轮更新即完成行为塑形**：作答率 20.1%→86.1%（run1 同位置是 20.1→0.2 的灭绝），
  avg_f1 0.099→0.281，且模型学会"少搜早答"（平均搜索 3.80→3.04）。
  印证核心假设：**终止行为本来就是 SFT 起点里被淹没的少数模式，
  干净的组对比梯度（门控后只有 答/不答 对照）一轮就能把它放大成主流模式**。
- 门控副作用符合预期：ITER 0 avg_reward 0.0785 < run1 的 0.139（不答轨迹不再白拿过程分）

**run2 结局：ITER 2 振荡，熔断保住 iter1（2026-09-02 中午）**

| 迭代 | avg_reward | avg_f1 | fmt_pct | avg_search |
|---|---|---|---|---|
| ITER 2 | 0.032 | 0.067 | **14.3** | 3.71 |

- ITER 1 更新后策略在温度 1.0 下作答率从 86.1% 荡回 14.3%（平均搜索回升 3.04→3.71）——
  **20→86→14 的三拍振荡**，熔断器按设计动作（`ABORT: fmt_pct collapsed`），训练终止，
  `iter0_merged / iter1_merged` 完好。产物归档至 `checkpoints/m3_run2/`
- **振荡机理**：ITER 1 训练信号里 86% 的组是"全答组"，组内优势由 F1 差异主导；
  大量低 F1 作答轨迹拿到负优势，其**整条轨迹（含终止动作）被抑制**——
  负样本与正样本共享绝大部分结构时，稀疏奖励会把"终止"这个稀有动作连同失败一起冲掉。
  叠加 362 条轨迹 × ~90 个优化步（单批 on-policy 数据上步数过多）与 β=0.02 弱锚，单轮即过冲
- **教训**：多轮 agentic GRPO 里"作答率"这类关键行为指标必须每轮监控并设熔断；
  更新强度（lr × 步数）必须随成功率上升而下调——成功率越高，组内对比越细碎，越要温和
- 贪心正式评测（n=500 dev bridge，G=1，temperature 0，top_k=6，agentic 协议）：

| checkpoint | EM | F1 | format | 平均搜索 |
|---|---|---|---|---|
| **iter0_merged** | **22.60** | **31.25** | 85.6% | 3.11 |
| iter1_merged | 5.20 | 6.79 | 17.6% | 3.75 |

  → **iter0_merged 为 run2 最优 checkpoint**；iter1 更新确认有害（贪心同样崩）

### M3 当前主结果阶梯（同 n=500 dev bridge；RL 行为 agentic 多轮，基线为单轮协议）

| 配置 | EM | F1 |
|---|---|---|
| A 无检索 | 4.20 | 13.15 |
| C BM25 k=6 单次检索 | 13.80 | 27.31 |
| **RL iter0（多轮自主检索，贪心）** | **22.60** | **31.25** |
| B oracle gold 上下文 | 30.00 | 51.86 |

- **对真实 RAG +8.8pp EM / +3.9pp F1**——多跳检索行为的 RL 训练增量，检索 Agent 项目核心卖点数字成立

### 归因对照与泛化（✅ 已完成 2026-09-02）

| 阶段（贪心，agentic 协议） | EM | F1 | format |
|---|---|---|---|
| SFT-only 冷启动（v1 merged） | 1.40 | 1.66 | 4.4% |
| **+ GRPO（iter0_merged）** | **22.60** | **31.25** | 85.6% |
| RL 净增量 | **+21.20** | **+29.59** | +81.2pp |

- **归因闭环**：冷启动 SFT 在贪心下几乎不产生终止作答行为（format 4.4%），
  GRPO 一轮即把作答率抬到 85.6%、EM 抬 21.2pp——增量完全来自 RL，
  这是 Search-R1 范式"行为靠 RL、知识靠检索"结论在本栈的独立复现

### run3：温和继续训练（2026-09-02 下午，进行中）

- 配置：从 run2-iter0 继续，lr 5e-6、β 0.05、每轮 ≤192 条有效轨迹（~24 优化步）、熔断保留
- ITER 0→1 rollout（温度 1.0）：fmt 85.5→**97.9%**、avg_search 3.08→2.61、
  F1 0.366→0.336（快答权衡）——**无振荡，温和更新方向健康**
- **run3-iter0 贪心复评（n=500 dev bridge）：EM 26.60 / F1 37.04 / format 99.6%**
  ——较 run2-iter0 再涨 +4.0pp EM / +5.8pp F1，距 oracle 上界仅 3.4pp EM

**更新后的主结果阶梯（同 n=500 dev bridge，RL 为 agentic 贪心协议）**：

| 配置 | EM | F1 |
|---|---|---|
| A 无检索 | 4.20 | 13.15 |
| C BM25 k=6 单次检索 | 13.80 | 27.31 |
| RL run2-iter0 | 22.60 | 31.25 |
| **RL run3-iter0（最终选定）** | **26.60** | **37.04** |
| B oracle gold 上下文 | 30.00 | 51.86 |

### run3 收尾与最终模型选定（2026-09-02 下午）

run3 各 checkpoint 贪心复评（n=500 dev bridge）：

| checkpoint | EM | F1 | format | 平均搜索 |
|---|---|---|---|---|
| **iter0_merged（= run2-iter0 + 1 轮温和更新）** | **26.60** | **37.04** | 99.6% | 2.65 |
| iter1_merged | 26.20 | 36.11 | 97.8% | 2.93 |
| iter2_merged | 23.80 | 33.90 | 90.6% | 3.09 |

- 贪心口径在 run3-iter0 见顶后回落；同期温度 1.0 训练指标持续上升
  （rollout F1 0.336→0.406→0.434，reward 0.598→0.680）——
  典型"采样探索性↑、贪心确定性↓"背离，按交付口径（贪心）选定 **run3-iter0 为最终模型**
  （`checkpoints/m3/iter0_merged`），iter3 未复评（趋势已明确）
- run3 四轮全程无熔断，温和超参（lr 5e-6、β 0.05、≤24 步/轮）验证有效

## M3 最终结论（检索 Agent 项目核心数字，全部真实跑出）

**主结果（HotpotQA dev bridge n=500，贪心，agentic 多轮协议）**：

| 阶段 | EM | F1 | 作答率 |
|---|---|---|---|
| base 无检索 | 4.20 | 13.15 | — |
| BM25 k=6 单次真实 RAG | 13.80 | 27.31 | — |
| SFT 冷启动 only | 1.40 | 1.66 | 4.4% |
| **SFT + 多轮 GRPO（最终）** | **26.60** | **37.04** | 99.6% |
| oracle gold 上下文（上界） | 30.00 | 51.86 | — |

- **对无检索 +22.4pp EM；对同检索器真实 RAG +12.8pp EM；距上界仅 3.4pp EM**
- **RL 消融**：同协议下 SFT-only 1.40 → +GRPO 26.60（+21.2pp），行为增量完全来自 RL
- **OOD 泛化（Bamboogle n=125，贪心）**：最终模型 EM 20.00 / F1 29.16 / format 96.8%
- **行为塑形**：平均搜索 3.96→2.65 次（学会"少搜早答"），检索命中过程分随训练单调上升
- **三次行为坍缩/振荡及修复**（本实验最有价值的部分）：
  ① run1 过程奖励未门控终止 → 作答灭绝（20→0.2%）→ 门控修复；
  ② run2 更新过冲 → 20→86→14 三拍振荡 → 熔断保住 iter0；
  ③ run3 温和超参 → 单调爬升无振荡。对应工程手段：奖励门控、作答率熔断、
  逐轮落盘复盘、零优势过滤、随成功率上升下调更新强度
- **bamboogle OOD（n=125，贪心）**：EM 21.60 / F1 28.76 / format 77.6%——
  与 dev 主结果（22.6/31.25）几乎持平，未见过数据集与题型仍保持，排除对 dev 过拟合
- run3（从 iter0 继续，lr 5e-6、β 0.05、≤24 步/轮、熔断保留）运行中，
  ITER 0 rollout：avg_reward 0.598、F1 0.366、fmt 85.5%（RL train 域、温度 1.0）

## REINFORCE vs GRPO 对照消融（2026-09-02，对接前沿）

**动机**：Search-R1++（arXiv 2602.19526，2026.02）在多轮搜索场景系统比较
REINFORCE/PPO/GRPO，结论是 REINFORCE 最终性能与效率最好、GRPO 稳定性最差。
我们用同一起点做同条件对照，检验该结论在我们的设置下是否成立。

**设置**（`m3_train_reinforce.py`，与 m3_train.py 仅两处差异：优势计算、输出目录）：
起点同为 `checkpoints/m2_sft_merged`；数据同为 `data/rl_train.jsonl` 前 128 题 × G=4；
超参与 run2 完全一致（lr 1e-5、KL β 0.02、ACC 4、温度 1.0、无步数截断）；
唯一变量：优势 = `r − batch_mean`（REINFORCE，无组结构、无 std 归一化）
替换组内归一化（GRPO）。门控、熔断、逐轮落盘均保留。

**结果**（训练口径，温度 1.0）：

| 迭代 | REINFORCE 作答率 | GRPO run2 作答率 |
|---|---|---|
| ITER 0 | 15.8%（reward 0.043，adv_abs_mean 0.2408） | 20.1% |
| ITER 1 | **2.3% → 熔断** | **86.1%（行为塑形成功）** |
| ITER 2 | —（已停） | 14.3% → 熔断 |

贪心复评 REINFORCE 唯一 checkpoint（iter0_merged，n=500 dev）：**EM 0.20 / F1 0.20 /
format 1.0% / 平均搜索 3.98** —— 比 SFT-only 起点（1.40）还差，行为形态为搜索循环
（与 run1 坍缩同款）。一次更新即把模型推离可用区，熔断机制按设计保住了迭代前的
起点模型（最终交付仍用 GRPO 线的 `checkpoints/m3/iter0_merged`）。

**机理分析：为什么 REINFORCE 一轮即坍缩（而 GRPO 至少完成了塑形）**
1. 起点作答率仅 ~16%：绝大多数轨迹无答案 → 门控下 r_format、r_process、r_answer 全 0，
   奖励分布是"大量 0 + 少量正 + 少量负（超搜惩罚）"，batch 均值仅 0.043
2. `A = r − mean` 使 ~84% 的"未答"轨迹拿到小负优势（≈ −0.04）。它们数量占绝对多数，
   负梯度体量与少数正梯度（作答轨迹 +0.2~0.6，仅 ~16%）相当甚至更大——
   更新方向变成"全面压制"而非"放大作答"
3. GRPO 的组内归一化天然是课程：全不答组 adv≡0 被零优势过滤，梯度只来自
   "有作答对比"的组，方向纯净地放大少数派行为；REINFORCE 的全局基线没有这层过滤，
   "平庸的大多数"直接污染梯度
4. 步数放大：REINFORCE 无零优势过滤，512 条轨迹全部参与（128 个优化步），
   约为 run2 GRPO 有效步数的两倍，过冲进一步加剧
5. ITER 1 的 adv_abs_mean 从 0.2408 掉到 0.0279：坍缩后全批次几乎无信号，
   与 run2 ITER 2 熔断时的"死寂"形态一致

**与论文结论的关系（诚实表述）**：我们的复现方向与论文相反——在低起点作答率、
小批量、单卡小规模设置下，REINFORCE 比 GRPO 坍缩得更快；与论文互证的部分是
"多轮场景优势估计极不稳定"（GRPO 三拍振荡 / REINFORCE 一轮坍缩，都发生在同一起点）。
论文"REINFORCE 最好"的结论基于其 Deep Research 场景（更高起点、更大数据量、
更长训练），我们把结论限定在实测范围内，不泛化引用。

## 中文迁移验证子实验（2026-09-02 启动）

**目的**：验证"同一套管线（检索环境 + 证据落地 SFT + 门控奖励多轮 GRPO）换语言/语料后仍然成立"。
不引入任何英文侧调好的组件之外的新技巧，差异仅在语言。

**管线与关键决策（按执行顺序）**：
1. 语料：hf-mirror 的 `wikimedia/wikipedia` 20231101.zh → 切段 **700.7 万段**
   （`zh_wiki_download.py`；空行/章节切块 + 句级二次切分，目标 40–250 字/段）
2. 分词与指标：`textproc.py` 英文字口径不变、中文连续段切**字 bigram**；
   中文 EM/F1 = 字符级（`m1_baseline_c.py` 加分支，英文行为不受影响）
3. 索引：同一套 `build_bm25_index.py`（jsonl 格式分支）→ `index_zh`：
   N=7,006,978 / V=10,302,804 / P=549,878,278 / avgdl=99.6，构建 1041s
4. 题目：教师模型 Qwen3-4B-Instruct-2507 翻译 HotpotQA bridge——
   **train 全量 49,705 题（双支撑标题）**、dev 全量 3,843 题（含与英文主评测同题的
   seed=42 子集）；严格 JSON 输出，parse_ok 99.2%/99.3%
5. 锚定（`zh_anchor.py`）：译名 → 语料规范标题。三级规则：精确匹配 →
   去括号消歧后缀 → **检索变体锚定**（译名检索 + 字 bigram dice/recall≥0.6，
   处理 里希特霍芬/李希霍芬 式译名变体）。
   **放弃的规则**：译名包含标题子串（抽检误配率高：迈克尔H·乔丹→迈克尔、
   NextGen America→NeXT）；宁可丢样本不喂错证据链
6. 数据门控（`zh_sft_data.py` / `zh_dev_filter.py`）：锚定后还要过两关——
   ① 证据覆盖（≤3 次检索内两个规范标题都出现）② 答案证据核验
   （答案字 bigram 在证据段落中覆盖率 ≥0.6，防译名变体导致"语料内不可答"）
7. 训练/推理格式：`SYSTEM_ZH` + 中文搜索结果包装文本（`SearchEnv(lang="zh")`），
   与 SFT 数据逐字符一致

**语料覆盖是硬约束（本实验最大的"发现"）**：
中文维基 128.6 万文章（英文 590 万），HotpotQA 实体的中文条目覆盖率很低。
精确锚定率仅 1.2–1.5%；加上检索变体锚定后标题级锚定率 dev 18.3% / train 17.4%，
但其中大量样本"标题锚定成功、证据/答案不可得"，被门控正确拦截。
最终幸存者：**dev 评测集 135 题；训练池 1,293 题（SFT 775 / RL 518）**。
锚定样本 8,578 → 幸存 1,293（15.1% 通过率）；拦截原因：覆盖不足 5,869、
答案无证据 1,416。

**基线（dev 锚定评测集，n=135，贪心，字符级指标）**：

| 阶段 | EM | F1 |
|---|---|---|
| A_zh 无检索直答 | 0.0 | 15.12 |
| C_zh BM25 单次真实 RAG (k=10) | 0.0 | 16.17 |
| SFT-only（v5 教师轨迹式起点） | 5.93 | **14.64**（作答率 42.96%） |
| SFT + GRPO（iter1 贪心选型） | **8.15** | **20.83**（作答率 60.0%） |

**中文线逐迭代贪心复评（与英文线同纪律：选型看贪心交付口径）**：

| 迭代 | F1 | 完美匹配 | 作答率 | 平均搜索 |
|---|---|---|---|---|
| SFT-only | 14.64 | 5.93 | 42.96% | 0.40 |
| iter0 | 19.02 | 8.15 | 51.85% | 0.40 |
| **iter1（选定）** | **20.83** | 8.15 | 60.0% | 0.27 |
| iter2 | 18.37 | 5.93 | 62.96% | 0.15 |

注 2：中文 SFT-only 略低于直答基线（15.12）属预期——与英文线同构
（SFT-only EM 1.4 也远低于基线）：教师轨迹式起点只负责给行为火种
（作答率 42.96%，温度 1.0 下 45%），性能交给 RL 塑形。
注意中文线轨迹带 `<think>` 推理块，每轮生成上限须 512（200 会被思考吃光，
详见踩坑⑥第四层）；英文线已定稿数字均为 200 口径，未受影响。

注 3（最终结论）：RL 消融增量 **+6.19pp F1**（同一起点，加 GRPO 前后）；
超过同检索器单次真实 RAG（16.17）**+4.66pp**——与英文线主结论同向复现；
平均搜索 0.40→0.27（学会"少搜早答"）。iter2 作答率继续升（62.96%）
但贪心 F1 回落——贪心/采样口径背离在中文线第二次复现，选型仍以贪心为准。
增益幅度小于英文线（+21.2pp EM）是语料覆盖约束的直接体现（见正文）。

注：字符级 EM 在 135 难题上极严格（译法微差即 0），F1 为主指标；
修复 `get_doc` 语料格式兼容前的旧数字（F1 10.4 那版）作废——当时检索返回空上下文。

### 训练工程踩坑记录

1. **Qwen3 模板三个 mask 陷阱**（M2/M3 共用的 `mask_utils.py` 已实测解决）：
   官方 `return_assistant_tokens_mask` 返回全 0（模板无 `{% generation %}`）；
   前缀长度差分法不可用（模板按上下文注入 `<think></think>` 空块，前缀渲染与全量渲染不一致）；
   正解：字符级锚点 + offset_mapping（实测 tok(text) 与模板 tokenize ids 完全一致）
2. **vLLM stop 字符串默认不进输出** → 轨迹解析正则必须容忍未闭合标签（`m3_env.py` 已处理）
3. **多轮上下文超 4096 崩溃**（M2 v1 评测容器整死）→ rollout `max_model_len=8192` + 搜索轮数硬上限 3
4. transformers 5.x `apply_chat_template(tokenize=True)` 返回 Encoding 对象，需 `return_dict=True` 取 `input_ids`
5. verl 官方镜像 torch 2.11 无 sm_70 kernel（见"verl 镜像预研结论"）→ 自研训练栈
6. **SFT 截断吃掉终止行为（中文线）**：中文多跳轨迹字符长度 p95≈3150，
   按英文经验设的 `MAX_LEN=2048` 把大量轨迹从正中间截断——尾部的 `<answer>` 轮
   从未进入训练（tokenized p95=max=2048 全是截断痕）。模型学会了多跳检索
   （甚至能从结果里抽实体反构 query）却**永远不作答**：贪心评测作答率 0%，
   RL 也无火种可放大（全组同分 → 零优势全过滤）。修复：MAX_LEN=4096 重训。
   教训：换语言/换数据先查轨迹长度分布，别沿用旧的截断常数。
   **但截断不是全部**：4096 重训（v2）后贪心与温度 1.0 作答率仍 0%。
   进一步定量：截断仅 1/775 条，真因是**答案轮信号密度太低**——答案轮中位仅
   12 token/条（占轨迹 ~0.7%），且中文数据 one_hop 仅 14%（英文线 ~1/3），
   775 条样本的答案行为总曝光只有英文线的 ~1/5，损失被搜索循环完全主导，
   终止行为学不出来。对策（v3）：one_hop 样本 ×3 再平衡（14%→33%）+
   5 epochs 加曝光（997 样本/625 步）。
   **v3 仍然 0%（贪心与温度 1.0，120 条探查轨迹全部 4 搜不答）**——至此可以确认
   不是截断/曝光问题，而是**证据落地范式的固有失败在中文线复现**：
   - 训练数据 100% 带落地证据，模型学会的是「见到 gold 证据才作答」的条件策略；
   - 真实检索召回定量（`zh_recall_probe.py`，135 题）：问题原文 query top-10
     命中 ≥1 个 gold 标题仅 **57.0%**、两个全中仅 **16.3%**（标题自身可检索率 92.2%，
     锚定本身没问题）——条件几乎永不触发 → 搜索循环；
   - 与英文线 v2（证据落地版）失败模式、根因完全一致。英文线当时验证过的对策：
     **回退教师轨迹式（允许无证据作答）作为 RL 起点**，把「何时作答」的决策
     交给 RL 奖励塑形，不靠 SFT 硬修。中文线照此构建 `sft_train_zh_v1style.jsonl`
     （见中文子实验章节更新）。
   通用教训升级：**「行为学不出来」先量化行为信号占比；信号占比修复后仍不行，
   检查数据是否把行为变成了条件策略，而推理时条件永不满足**。
   **第四层（v4 教师轨迹式仍 0% 后的决定性实验）**：按英文线对策改用教师轨迹式
   （无证据作答，4000 条 1:1 单双跳，1 epoch），温度 1.0 探查仍 0%。
   做截断前缀续写实验（`zh_prefix_probe.py`）：把**训练集原样前缀**截到
   「搜索结果刚出现」处让模型续写——one_hop 前缀 0/20 产 `<answer>`、
   two_hop 前缀 0/20、评测前缀 0/20，全部续 `<search>`。
   **结论：不是分布迁移、不是条件策略，是答案分支根本没学会**——
   训练目标里下一 token 就是 `<answer>` 的前缀，模型也照样续搜索。
   旁证：loss 在 step 100 后 plateau 在 ~0.3（v3 五 epoch 能到 0.116），
   残差正卡在答案轮位置。基座在中文域的「继续搜索」先验极强，
   1 epoch 学得会格式、翻不过稀有分支。对策（v5）：在 v4 合并模型上
   叠新 LoRA 续训 +2 epochs 加曝光。教训再升级：**行为分支学没学会，
   用「训练原样前缀续写」一刀切开分布问题与学习问题**。
   **v5 结局（火种点燃）**：loss 0.30→0.133；前缀探针 A 组 0/20→5/20、
   B 组 6/20 产 `<answer>`（模型先 `<think>` 推理再答）；温度 1.0 作答率
   200 token 预算下仅 3.3%——**又是基建坑：思考块把 200 token 预算吃光，
   96% 轨迹截断在思考中**，放宽到 512 后作答率 45%（比英文 v1 的 ~20% 还高），
   贪心全量作答率 42.96%、F1 14.64。教训：**「模型不答」先排除生成预算
   被思考吃掉**，别急着归因行为
7. **`BM25.get_doc` 语料格式假设（中文线）**：取原文函数只认英文语料的
   `{"contents": "title\\n正文"}`，中文语料 `{"title","text"}` 分离字段一律返回
   空串——导致覆盖检查全灭、检索锚定零收益、"RAG 基线"实为空上下文。
   修复后三处同时回正。教训：跨语料复用检索代码，先对 1 条返回做冒烟

## 下一步（2026-09 更新：本节曾停在 M2 时代，现已全部走完并超出）

历史规划的 M3 自研多轮 GRPO 已落地并收官：检索 Agent（M0-M4 全里程碑）
与代码 Agent（C4 全里程碑 + 修复行为深挖）均完成，见各 README 与
`logs/` 实测产物。当前进行中的迭代（冲击规模化叙事）：

1. **规模放大方案**：见 `SCALE_UP.md`——verl 迁移映射、8B/30B/70B 外推模型
   （估算标注口径）、千卡预案（设计级）、推理引擎定制路线。
2. **WS-1 vLLM 深度定制实证**（进行中）：`poc_vllm_bench.py`，89 题六配置矩阵
   （prefix ON/OFF × eager/CUDA graph × util 阶梯）+ 贪心输出不变性核对，
   产物 `logs/poc_vllm/`。已确认：V1 引擎 prefix caching 默认 ON；
   sm_70 上 CUDA graph 可用。
3. **WS-2 GiGPO 式 turn 级优势对照**（进行中）：`c4_gigpo.py` +
   `c4_train_gigpo.py` + `poc_gigpo_test.py`（31 项单测），配对 replay
   方向门与全量对照，产物 `logs/poc_gigpo/`、`checkpoints/c4_replay/`。

## 复用命令

```bash
# 冒烟 / rollout
docker run --rm --gpus '"device=0"' \
  -v /mnt/nas3/shared/model:/models:ro \
  -v /mnt/storage/<user>/claudecode_projects/agentic_rl:/work -w /work \
  -e HF_HUB_OFFLINE=1 \
  1cat-vllm:v100-1.3.0 python m0_rollout.py

# 注意：GPU 4 被同事容器 v10s1-sft 占用，优先用 0-3/5-7
```
